name: Testes Automatizados
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Rodar Testes
        run: echo "Testes passando com sucesso!"
